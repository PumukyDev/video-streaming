services:
  node:
    image: node:23.6.1-alpine3.20
    container_name: node
    working_dir: /app
    volumes:
      - .:/app
    command: ["node", "server.js"]
    ports:
      - "5000:5000"
    networks:
      - app_network
  web:
    image: httpd:latest
    container_name: web
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
